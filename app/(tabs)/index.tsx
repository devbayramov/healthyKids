import { ScrollView } from "react-native";

export default function Home() {
  return (
    <ScrollView contentContainerStyle={{ gap: 20, padding: 10, backgroundColor: "#D1DEBE" }}>
      {/* Home content */}
    </ScrollView>
  );
}

